// The client should be able to receive raw blocks as input. Note that it can only help downloaders
// to distribute raw transactions. Wallets are not supported.

pub struct ClientState;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
    }
}
